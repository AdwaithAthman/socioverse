export interface ProfileInterface {
    userId?: string,
    name?: string,
    username?: string,
    email?: string,
    phoneNumber?: number,
    bio?: string,
    gender?: string,
    city?: string,
    savedPosts?: string[],
}